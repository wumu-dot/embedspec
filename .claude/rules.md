# AI 硬规则（会话/子Agent自动继承，强制遵守）

[R0] 新会话、派生子Agent启动自动加载本规则全集，无需额外技能调用。

[R1] 读取docs/下任意文档前必须读取INDEX.md，同一会话仅读取一次缓存复用。

[R1.1] 编码任务匹配INDEX场景地图，批量读取全部关联文档后再设计方案。

[R2] 修改docs目录文档，同步更新INDEX.md对应条目更新日期。

[R3] 用户反馈程序运行异常、死机，优先读取troubleshooting/bug_shturl检索历史案例。

[R4] 纯新增业务开发、无报错反馈时，禁止读取bug_shturl减少token消耗。

[R5] 故障修复并通过CI校验后，按标准模板追加bug记录，严禁删除、修改历史条目。

[R6] 写入bug日志后读取文件末尾50行校验完整性，内容截断则重新追加。

[R7] 编写RCC时钟、FreeRTOS、DMA、中断优先级代码前，必读summary/lessons_summary.md。

[R8] 生成代码前置三层校验：优先复用HAL标准库→复用现有BSP模块→grep检索调用链，无复用才编写最小代码。

[R9] 正式编码前输出《改动影响地图》：变更文件、宏/结构体/接口、上下游调用、验证方案；输出后暂停，等待用户「确认/修改调整」才可编写代码。

[R10] 修改已有模块前检索全项目调用关系（grep -r 函数名），确认影响范围。

[R11] 任意代码修改完成后必须运行 ./scripts/ci_local.sh，脚本非0退出码不得标记开发完成。

[R12] 硬件报错调试场景，从docs/tools/gdb_debug.md提取对应GDB调试指令提供给用户。

[R13] 开发、测试、评审三类工作拆分独立会话，同一上下文禁止切换角色。

[R14] 单模块编码+CI自检完成，输出固定标记 ⏸️ 等待用户烧录/确认，无「继续」指令不推进下一阶段。

[R15] bug_shturl累计新增5条记录，会话启动主动提醒人工提炼通用规范写入lessons_summary.md。

[R16] CI校验全部通过，用户需硬件验证时主动推荐/flash一键烧录技能。

[R17] 设备死机、运行异常优先推荐/debug GDB调试技能，告知抓取backtrace用于bug归档。

[R18] CI因堆栈/HSE晶振自动化规范校验失败，优先定位违规配置针对性修改，禁止无方向反复调试。

[R19] 禁止修改底层库文件（Core/、Drivers/、lvgl/、Middlewares/）；App层开发仅通过配置文件和适配层接口。

[R20] 修改任何引脚前必看 hal_msp.c，引脚宏定义必须与之一致。
