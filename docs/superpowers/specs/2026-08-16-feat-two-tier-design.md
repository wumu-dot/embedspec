# FEAT 两级体系设计（父FEAT + 子FEAT）

- **日期**：2026-08-16
- **状态**：已确认
- **适用范围**：embedspec 可复用标准（复制到任意项目使用）

## 1. 背景与动机

现有 FEAT 体系只有一层：`docs/features/.template.md`（5 阶段：准备→设计→实现→测试→审查），
用于单次功能开发。但真实项目有**多级粒度**：一个大目标（如完整产品）→ 若干小阶段（里程碑）→ 每个里程碑内多个具体步骤。

用户需要一个**两级 FEAT 体系**：

```
大阶段（项目级目标）  →  项目本身，由 README / HANDOFF / docs/INDEX 承载，不建文档
        │
        ▼
小阶段 = 父FEAT       →  A1 / A2 / A3 …（统筹规划 + 项目表）
        │
        ▼
步骤 = 子FEAT         →  A1-01 / A1-02 …（每个走5阶段，带验收标准）
```

示例（SoundDog 项目，仅作演示，非模板内容）：
- A1 麦克风验证 → A1-01 接线验证、A1-02 I2S采集、A1-03 采样率校验…
- A2 RS485通信 → A2-01 硬件接口、A2-02 收发状态机、A2-03 协议帧…
- A3 上位机 → A3-01 框架+串口、A3-02 协议解析、A3-03 波形显示…

## 2. 核心设计决策（已确认）

| # | 决策点 | 结论 |
|---|--------|------|
| D1 | 文件组织 | 父FEAT = 计划表文档；子FEAT = 独立文档走5阶段 |
| D2 | 命名 | 父 `FEAT-A1-描述`；子 `FEAT-A1-01-描述`（字母=小阶段/目标，数字=子步骤序号） |
| D3 | 子FEAT流程 | 完整5阶段（复用现有模板）+ 停等 + 12条AC |
| D4 | 模板方案 | 方案A：独立 `.template-parent.md` + `.template-child.md`（原 `.template.md` 改名） |
| D5 | 可复用性 | 模板一律项目无关（`{{占位符}}`）；示例独立放 `examples/` 并标注"演示用，可删" |
| D6 | 示例内容 | 用 SoundDog A1/A2/A3 三组演示完整生命周期（不用做饭类比） |
| D7 | 大阶段归属 | 不建文档，落在 README（项目说明/当前状态/遗留问题）+ HANDOFF + docs/INDEX |
| D8 | 父FEAT状态 | 由项目表汇总：全子🟢→父🟢；有子🟡→父🟡；无子开工→父🔴 |
| D9 | 父FEAT生命周期 | 规划 → 人确认方向 → 逐个开子FEAT → 子阶段5通过回填 → 全🟢置父🟢 |

## 3. 文件布局

```
docs/features/
├── .template.md            → 改名 .template-child.md（子FEAT：原5阶段 + 父引用 + 回填规则）
├── .template-parent.md     → 新增（父FEAT：方向 + 项目表 + 依赖 + 阻塞 + DoD）
├── INDEX.md                → 两级索引模板
└── examples/               → 演示用，复制到项目后删除或改写
    ├── FEAT-A1-麦克风验证.md
    ├── FEAT-A1-01-接线验证.md
    ├── FEAT-A2-RS485通信.md
    └── FEAT-A3-上位机.md
```

> 示例为最小演示集：A1 父 + 1 个子FEAT 走完整5阶段；A2、A3 父FEAT只展示项目表结构。

## 4. 父FEAT模板 `.template-parent.md`（角色：Planner）

| 章节 | 内容 |
|------|------|
| 0. 元数据 | 优先级 / 预估总耗时 / 状态（🔴规划中→🟡部分完成→🟢全部完成） |
| 1. 总体目标与方向 | 核心目标一句话 + 大方向策略 + 硬边界（继承 CLAUDE.md） |
| 2. 项目表（子FEAT清单） | 子FEAT编号 / 功能 / 验收要点 / 优先级 / 依赖 / 状态 / 文件 |
| 3. 依赖与执行顺序 | 谁先谁后、可并行项 |
| 4. 阻塞与下一步 | 等硬件 / 待决策 / 下一步动作（对应"还需要我做什么？"） |
| 5. 完成定义（DoD） | 项目表全部🟢 = 父FEAT完成 |
| 6. 执行日志 | 时间/阶段/角色/摘要/遇阻 |

AI 执行规则：父FEAT 只做规划与统筹，不写实现代码；规划完成停等，人确认方向后才开子FEAT。

## 5. 子FEAT模板 `.template-child.md`（角色：Dev/Test/Review）

= 现有 `.template.md` 全部保留（5阶段 + 停等 + 12条AC），仅增量：

- 元数据新增一行：**父FEAT**：`FEAT-XX-…`（无父则填 `无（独立FEAT）`，保证单层项目也能用）
- 标题格式改为：`FEAT-A1-01-{简短描述}`（字母段继承父FEAT编号）
- 顶部规则新增一句：**阶段5通过后 → 回填父FEAT项目表该行 + 更新 INDEX.md**

## 6. INDEX 两级索引

```
| 层级 | FEAT ID | 优先级 | 描述 | 状态 | 文件 |
| 父   | FEAT-A1 | P1 | 麦克风验证 | 🟡 | … |
| ├子  | FEAT-A1-01 | P1 | 接线验证 | 🟢 | … |
| ├子  | FEAT-A1-02 | P1 | I2S采集 | 🔴 | … |
| 父   | FEAT-A2 | P1 | RS485通信 | 🔴 | … |
```

- 父子层级用 `├子` 前缀缩进表示
- 父行状态由子行推导（D8）

## 7. 状态流转

```
创建父FEAT（规划）
  → ⏸️ 人确认方向
  → 按项目表逐个创建子FEAT（各走5阶段，每阶段停等）
  → 子FEAT 阶段5通过 → 更新 INDEX 子行 + 父FEAT项目表该行
  → 项目表全部🟢 → 父FEAT置🟢（DoD达成）
```

## 8. 引用更新

| 文件 | 更新内容 |
|------|----------|
| `CLAUDE.md` | 场景导航表：规划方向→`.template-parent.md`；实现功能→`.template-child.md` |
| `docs/INDEX.md` | 场景表 + 文档清单：`features/.template.md` → 父子两模板 + INDEX + examples |
| `README.md` | 目录树 + 核心流程：补两级体系说明 |

## 9. AI 约束与规范（执行 Agent 强制）

**基础继承**（会话/子Agent自动加载）：`.claude/rules.md` R0-R20 + CLAUDE.md 硬性边界。
与两级体系强相关的规则：R9（改动影响地图）、R11（CI 必跑，非0不得标完成）、R13（角色分离）、
R14（⏸️停等，无「继续」不推进）、R19（地基禁碰即暂停）、R2/R6（INDEX同步/日志完整性）。

### 9.1 角色分离（R13）
- 四类角色：**Planner**（父FEAT规划）/ **Dev**（子FEAT阶段1-3）/ **Test**（子FEAT阶段4）/ **Review**（子FEAT阶段5）。
- 同一上下文禁止切换角色；子FEAT的阶段4/5 由独立会话的 Test / Review Agent 执行。

### 9.2 父FEAT（Planner）约束
- **只做统筹规划，禁止写实现代码**；实现一律拆成子FEAT。
- 创建/修改项目表后停等，**人确认方向后才允许开子FEAT**。
- **禁止自审自判完成**：父FEAT置🟢仅当项目表全部子FEAT🟢，且每个🟢都经过 Review。

### 9.3 子FEAT（Dev/Test/Review）约束
- 继承现有模板全部规则：阶段顺序、角色声明、停等、严禁跳跃/越界/自审自判。
- R9 影响地图（阶段1输出）；R11 CI必跑（阶段4）；R14 ⏸️停等标记；R19 地基触碰即暂停进地基评估。
- **阶段5 Review 通过 = 唯一完成判据**；AI 不得自行标记通过。

### 9.4 表格与状态维护
- 子FEAT 阶段5通过后，AI 回填**父FEAT项目表该行 + INDEX.md 子行**；回填内容必须与子FEAT实际状态一致，禁止预填/伪造。
- 父行状态由子行推导（D8），AI 不得改写父行状态使其与推导结果矛盾。
- 改 docs 后按 R2 同步 INDEX 更新日期；执行日志按 R6 校验完整性。
- 历史条目只更新状态字段，**严禁删除/篡改已完成记录**（与 R5 精神一致）。

### 9.5 模板落地形式
- 父模板顶部「AI 执行规则」块：只规划不实现、开子FEAT前停等确认。
- 子模板顶部「AI 执行规则」块：继承原5阶段规则 + 回填义务（阶段5通过 → 回填父表 + INDEX）。

## 10. 兼容性

- 已复制 embedspec 的项目：`.template.md` 改名 `.template-child.md` 是破坏性变更，但子模板内容与原名几乎一致（仅新增2处），迁移成本极低
- 单层项目仍可用：子FEAT填"无（独立FEAT）"即等价于旧模板
- 不涉及源码/脚本/CI，`check-doc-drift.sh` 不受影响

## 11. 验收标准

- [ ] 父/子模板各一份，项目无关（无 STM32/做饭/SoundDog 字样）
- [ ] 子模板含原5阶段 + 12条AC + 父引用 + 回填规则
- [ ] 父模板含项目表 + 依赖顺序 + 阻塞下一步 + DoD
- [ ] 模板含完整 AI 约束：角色分离 / 只规划不实现 / 停等确认 / 防自审自判 / 回填纪律 / 历史只追加（对应第9节）
- [ ] INDEX 两级索引模板 + 状态图例 + 父行推导规则
- [ ] examples/ 演示 A1父+A1-01子完整流程、A2/A3父结构，头部标注可删
- [ ] CLAUDE.md / docs/INDEX.md / README.md 引用同步更新

## 12. 扩展：维护与快速定位（v1.1，2026-08-16 批准）

> 目标：功能做完后，文档仍能支撑后期维护与调试——出问题时快速定位到 FEAT、调试要点与测试入口。
> 采用机制（用户选定）：**FEAT 维护卡片 + 反向索引维护地图**。不纳入：BUG↔FEAT 互链、模块接口定义强化（YAGNI）。

### 12.1 子FEAT 模板新增「维护与调试」章节

插入位置：「3. 验收标准」之后，原「4. 执行日志」顺延为 5。

```
## 4. 维护与调试（阶段5通过后回填）

### 4.1 影响文件
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| {{路径}} | 新增/修改 | {{改动内容}} |

### 4.2 调试要点
- {{调试手段：GDB断点 / 日志开关 / /debug 技能 / 验证命令}}

### 4.3 测试入口
- {{测试命令或用例位置，对应 AC-XX}}
```

### 12.2 顶部回填规则与阶段5 步骤扩展

- 顶部 AI 执行规则回填行：`回填父FEAT项目表该行（独立功能跳过）+ 更新 docs/features/INDEX.md + docs/features/维护地图.md`
- 阶段5 通过后动作：回填「维护与调试」章节 + 父FEAT项目表 + INDEX + 维护地图

### 12.3 新增 `docs/features/维护地图.md`（文件 → FEAT 反查）

```
# 维护地图（文件 → FEAT 反查）

> 用法：出问题时先定位到文件/模块 → 查本表 → 读关联FEAT 的「维护与调试」章节 → 拿调试要点与测试入口。
> 子FEAT 阶段5通过后由 AI 追加一行。

| 文件/模块 | 关联FEAT | 功能简述 | 状态 |
|-----------|---------|---------|------|
| {{路径}} | FEAT-A1-01 | {{简述}} | 🟢 |
```

### 12.4 快速定位路径（写入场景导航）

```
出问题 → 1) 查 docs/troubleshooting/bug_shturl（历史故障）
       → 2) 定位文件/模块 → 查 docs/features/维护地图.md → 关联FEAT 的「维护与调试」
       → 3) 读「调试要点」→ GDB//debug 排查；读「测试入口」→ 回归验证
```

### 12.5 引用同步

| 文件 | 更新 |
|------|------|
| `CLAUDE.md` | 场景导航 +1 行：出问题快速定位 → 维护地图 |
| `docs/INDEX.md` | 场景表 +1 行、文档清单 +维护地图行 |
| `README.md` | 目录树 +维护地图行 |
| `docs/features/INDEX.md` | 维护规则补"回填维护地图" |
| `examples/FEAT-A1-01-接线验证.md` | 补「维护与调试」章节（演示） |
| `examples/维护地图示例行` | 维护地图补 A1-01 示例行 |

### 12.6 验收（v1.1）

- [ ] 子模板含「维护与调试」章节（4.1影响文件/4.2调试要点/4.3测试入口），执行日志顺延为 5
- [ ] 顶部回填规则与阶段5 动作含维护地图
- [ ] `维护地图.md` 存在，含用法说明 + 表头 + 示例行
- [ ] 四文件引用同步（CLAUDE.md / docs/INDEX.md / README.md / features/INDEX.md）
- [ ] examples/A1-01 演示维护卡片；维护地图含 A1-01 示例行
- [ ] 项目无关性保持（模板无具体项目词）
