# FEAT-{A1}-{序号}-{简短描述}

> 无父FEAT 的独立功能用：`FEAT-{YYYYMMDD}-{序号}-{简短描述}`。

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行（独立功能跳过）+ 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。

## 0. 元数据
- **父FEAT**：FEAT-{A1}-{父描述}（独立功能填"无"）
- **优先级**：P0(阻断) / P1(紧急) / P2(正常) / P3(可缓)
- **预估总耗时**：
- **当前状态**：🔴未开始 / 🟡阶段X进行中 / 🟢已完成

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）
> 示例：实现 {{模块}} 的 {{能力}}，支撑 {{大阶段目标}}。（父FEAT 填功能域，子FEAT 填具体模块）

### 1.2 地基触碰前置自检
> 以下执行路径是否涉及 CLAUDE.md "硬性边界" 中定义的底层库？

- [ ] 是 → 立即进入地基评估流程，本 FEAT 暂停
- [ ] 否 → 继续进入下方执行路线图

### 1.3 任务边界
| 类型 | 具体内容 |
|------|----------|
| 范围内（可改） | |
| 超出范围（禁止碰） | 底层库 / 第三方库 |
| 外部依赖 | |

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）
- **阶段目标**：读相关文件，分析依赖，输出影响范围
- [ ] 步骤1：定位涉及的文件和接口
- [ ] 步骤2：输出影响范围（修改/新增/接口变化/兼容性/风险）
- **⏸️ 停等：人确认后进入阶段 2**

### 🔵 阶段 2：设计（角色：Dev Agent）
- **阶段目标**：设计数据结构、接口、模块划分
- [ ] 步骤1：关键数据结构设计
- [ ] 步骤2：接口/函数签名设计
- **⏸️ 停等：人确认后进入阶段 3**

### 🟠 阶段 3：实现（角色：Dev Agent）
- **阶段目标**：写代码，每改完一个文件对照 AC 自检
- [ ] 步骤1：
- [ ] 步骤2：
- **⏸️ 停等：人确认后进入阶段 4**

===== 切换至 Test Agent =====

### 🟣 阶段 4：测试（角色：Test Agent）
- **阶段目标**：独立测试——只看规格，不看 Dev 的实现细节
- [ ] 步骤1：编写测试用例，逐条验证 AC
- [ ] 步骤2：运行 `./scripts/ci_local.sh`，输出报告
- [ ] 若失败 → 退回阶段 3
- **⏸️ 停等：人确认后进入阶段 5**

===== 切换至 Review Agent =====

### 🔴 阶段 5：审查（角色：Review Agent）
- **阶段目标**：独立审查——安全检查、代码质量、规范合规、AC 逐条打勾
- **Review 通过 = 功能完成。** 不通过 → 退回对应阶段。
- 若全部通过 → ✅ 完成，回填「维护与调试」章节 + 父FEAT项目表（如有）+ 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`
- 若有未通过 → 退回 Dev Agent 并附意见

## 3. 验收标准（12 条）

### 功能验收
- [ ] AC-01 功能目标达成
- [ ] AC-02 向后兼容

### 代码质量
- [ ] AC-03 编译/Lint 通过
- [ ] AC-04 无新增警告
- [ ] AC-05 文档已更新

### 安全红线
- [ ] AC-06 无硬编码密钥
- [ ] AC-07 错误处理完善
- [ ] AC-08 用户输入已校验

### 测试与边界
- [ ] AC-09 新增测试自动运行
- [ ] AC-10 全部回归测试通过
- [ ] AC-11 测试覆盖率 ≥ 80%
- [ ] AC-12 按 FEAT 模板执行

## 4. 维护与调试（阶段5通过后回填）

### 4.1 影响文件
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| {{路径}} | 新增/修改 | {{改动内容}} |

### 4.2 调试要点
- {{调试手段：GDB断点 / 日志开关 / /debug 技能 / 验证命令}}

### 4.3 测试入口
- {{测试命令或用例位置，对应 AC-XX}}

## 5. 执行日志

| 时间 | 阶段 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| | | | | |
